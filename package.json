{
  "name": "laverit",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "tsnd --respawn --transpile-only --exit-child src/server.ts",
    "build": "NODE_ENV=production --respawn --transpile-only --exit-child src/server.ts",
    "test": "NODE_ENV=test npx jest --verbose --runInBand"

  },
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^4.8.1",
    "@types/jest": "^29.2.6",
    "fastify": "^4.11.0",
    "fastify-jwt": "^4.2.0",
    "fastify-swagger": "^5.2.0",
    "fastify-zod": "^1.2.0",
    "jest": "^29.3.1",
    "morgan": "^1.10.0",
    "pino-pretty": "^9.1.1",
    "ts-jest": "^29.0.5",
    "ts-node-dev": "^2.0.0",
    "zod": "^3.20.2",
    "zod-to-json-schema": "^3.20.2"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "typescript": "^4.9.4"
  }
}
